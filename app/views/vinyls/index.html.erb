<!-- render '_search' -->
<% if @vinyls.length > 0 %>
<%  @vinyls.each do |vinyl| %>
  <%= link_to vinyl_path(vinyl) do %>
    <div class="card-product">
      <!-- vinyl.photo -->
      <% if vinyl.photo.attached? %>
        <%= cl_image_tag vinyl.photo.key, width: 110, height: 110, crop: :fill %>
      <% else %>
        <%= image_tag "https://via.placeholder.com/110" %>
      <% end %>
      <!-- delete if when seeds.rb work -->
      <div class="card-product-infos">
        <h2><%= vinyl.title %></h2>
        <p><%= vinyl.artist %></p>
      </div>
      <div class="card-product-price">
        <p><%= vinyl.price_per_day %>€</p>
      </div>
    </div>
  <% end %>
<% end %>

<% end %>

<% if @title.length > 0 %>
  <% @title.each do |vinyl| %>
    <%= link_to vinyl_path(vinyl) do %>
      <div class="card-product">
        <!-- vinyl.photo -->
        <% if vinyl.photo.attached? %>
          <%= cl_image_tag vinyl.photo.key, width: 110, height: 110, crop: :fill %>
        <% else %>
          <%= image_tag "https://via.placeholder.com/110" %>
        <% end %>
        <!-- delete if when seeds.rb work -->
        <div class="card-product-infos">
          <h2><%= vinyl.title %></h2>
          <p><%= vinyl.artist %></p>
        </div>
        <div class="card-product-price">
          <p><%= vinyl.price_per_day %>€</p>
        </div>
      </div>
    <% end %>
  <% end %>

<% end %>

<% if @title.length == 0 and  @vinyls.length == 0 %>

  Sorry we didn't find your Request

<% end %>
